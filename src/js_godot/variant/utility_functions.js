import { StringName } from 'src/js_godot/variant/string_name'
import { Variant } from 'src/js_godot/variant/variant'

export class GD {
  static print (...args) {
    // ret = new Variant()
    // print_function(ret, args, args.length)
  }
}
